# compose.prod.yml - Docker Compose overrides for production environment

services:

  backend:
    build:
      target: prod
    restart: unless-stopped
    env_file:
      - backend/.env.prod
    

  backend-migrations:
    build:
      target: db-ops
    env_file:
      - backend/.env.prod

  database:
    env_file:
      - backend/.env.prod
